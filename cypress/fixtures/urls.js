const baseUrl = Cypress.config().baseUrl;
const apiBaseUrl = `${baseUrl}api/`;

export const URL = `${baseUrl}system-configuration/object-classes/create`;
export const URL_LOGIN_API = `${apiBaseUrl}users/login/`;
